# speakr-mcp
Sidecar MCP server for Speakr (self-hosted meeting recorder): exposes the Speakr REST API as native MCP over Streamable HTTP. Runs on the same Docker network as Speakr — no SSO/internet round-trip.
